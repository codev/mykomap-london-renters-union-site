{
  "name": "mykomap-london-renters-union-site",
  "version": "0.1.0",
  "description": "London Renters Union - London property licensing map",
  "main": "src/index.ts",
  "config": {
    "deploy_to": "",
    "deploy_user": "www-data",
    "deploy_group": "www-data"
  },
  "scripts": {
    "build": "mykomap-site-build -c config -s src -m node_modules/mykomap -d build www/*",
    "deploy": "mykomap-site-deploy -f build/out/",
    "server": "php -t build/out -S localhost:8080",
    "public-server": "echo \"VIEW THE APP AT: http://$(hostname -I | tr -d ' '):8080\";php -t build/out -S 0.0.0.0:8080"
  },
  "author": "Digital Commons Cooperative",
  "license": "UNLICENSED",
  "repository": "github:Codev/mykomap-london-renters-union-site",
  "dependencies": {
    "mykomap": "github:DigitalCommons/mykomap#semver:^3.1.4"
  }
}
